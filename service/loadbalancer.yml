apiVersion: v1
kind: Service
metadata:
  name: loadbalancer-service
  namespace: test
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
